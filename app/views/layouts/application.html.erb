<!DOCTYPE html>
<html>
<head>
      <script src="http://d3js.org/d3.v3.min.js"></script>
      <%= stylesheet_link_tag    "application", media: "all"%>
      <%= javascript_include_tag "application", type: "text/javascript" %>
      <link href='http://fonts.googleapis.com/css?family=Lato:300,700,900' rel='stylesheet' type='text/css'>
      <link href='http://fonts.googleapis.com/css?family=Josefin+Slab:300' rel='stylesheet' type='text/css'>
      <!--  font-family: 'Josefin Slab', serif;  -->

  <title>Collabstoryproject</title>

  <%= csrf_meta_tags %>
  <div id="header_bar">
    <object>
    	<% logged_in = (session[:id] != nil) %>
    	<% if logged_in %>
    		Hi <%= User.find_by_id(session[:id]).first_name %>! &#160; | &#160;
        <%= link_to ("Index"), :controller => "stories", :action => "index" %> &#160; | &#160; 
    		<%= link_to ("LOGOUT"), :controller => "users", :action => "logout" %>
    	<% else %>
    		<%= link_to ("LOGIN"), :controller => "users", :action => "login" %>
    	<% end %>
   </object>
</div>
</head>
<body>

<%= yield %>

</body>
</html>
